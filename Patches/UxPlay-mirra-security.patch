diff --git a/lib/pairing.c b/lib/pairing.c
--- a/lib/pairing.c
+++ b/lib/pairing.c
@@ -488,7 +488,8 @@ random_pin() {
     int ret = 0;
     /* create a random unsigned short in range 1-9999 */
     while (!random_short) {
-        if ((ret = get_random_bytes(random_bytes, sizeof(random_bytes))  < 1)) {
+        ret = get_random_bytes(random_bytes, sizeof(random_bytes));
+        if (ret < 1) {
             return -1;
         }
          memcpy(&random_short, random_bytes, sizeof(random_bytes));
diff --git a/lib/raop_handlers.h b/lib/raop_handlers.h
--- a/lib/raop_handlers.h
+++ b/lib/raop_handlers.h
@@ -261,6 +261,7 @@ raop_handler_pairpinstart(raop_conn_t *conn,
         pin_4 = random_pin();
         if (pin_4 < 0) {
             logger_log(raop->logger, LOGGER_ERR, "Failed to generate random pin");
+            return;
         } else {
             raop->pin = (unsigned short) pin_4 % 10000;
         }
@@ -270,7 +271,7 @@ raop_handler_pairpinstart(raop_conn_t *conn,
     if (raop->callbacks.display_pin) {
          raop->callbacks.display_pin(raop->callbacks.cls, pin);
     }
-    logger_log(raop->logger, LOGGER_INFO, "*** CLIENT MUST NOW ENTER PIN = \"%s\" AS AIRPLAY PASSWORD", pin);
+    logger_log(raop->logger, LOGGER_INFO, "AirPlay verification code generated");
 }
 
 static void
@@ -665,7 +666,7 @@ raop_handler_setup(raop_conn_t *conn,
                 int pin_4  = random_pin();
                 if (pin_4 < 0) {
                     logger_log(raop->logger, LOGGER_ERR, "Failed to generate random pin");
-                    pin_4 = 1234;
+                    return;
                 }
                 if ((raop->random_pw =  (char *) calloc(pin_len + 1 + 18, sizeof(char)))) {
                     char *pin = raop->random_pw;
@@ -681,7 +682,7 @@ raop_handler_setup(raop_conn_t *conn,
                 if (raop->callbacks.display_pin) {
                     raop->callbacks.display_pin(raop->callbacks.cls, raop->random_pw);
                 }
-                logger_log(raop->logger, LOGGER_INFO, "*** CLIENT MUST NOW ENTER PIN = \"%s\" AS AIRPLAY PASSWORD", raop->random_pw);
+                logger_log(raop->logger, LOGGER_INFO, "AirPlay verification code generated");
                 raop->auth_fail_count++;
             }
 	    if (len && !conn->authenticated) {
